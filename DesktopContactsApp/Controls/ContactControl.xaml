<UserControl x:Class="DesktopContactsApp.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopContactsApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid Background="LightCyan">
        <StackPanel Margin="5">
            <TextBlock x:Name="nameTextBlock" Text="Name" FontSize="20" FontWeight="Bold" />
            <TextBlock x:Name="emailTextBlock" Text="Email" FontSize="15" Foreground="DodgerBlue" />
            <TextBlock x:Name="phoneTextBlock" Text="Phone" FontSize="15" FontStyle="Italic" />
        </StackPanel>
    </Grid>
</UserControl>
